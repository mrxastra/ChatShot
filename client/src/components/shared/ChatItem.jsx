import React from 'react'
import ChatList from '../specific/ChatList'

const ChatItem = ({
  avatar = [],
  name,
  _id,
  groupChat = false,
  sameSender,
  isOnline,
  newMessage,index=0,
  handleDeleteChatOpen,
}) => {
  return (
    <Link to={"/"}>
      <div className="flex gap-2 align-middle p-3 bg-black text-white relative">
        <stack>
          2222222
        </stack>
      </div>
    </Link>
  )
}

export default ChatItem